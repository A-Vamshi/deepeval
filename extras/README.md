# extras/

Parked code not part of the active build. Moved here to keep V1 focused.

- `metric_templates/community/` + `cli/translate/` — the multi-language metric
  template localization layer (the `deepeval translate` CLI, community/custom
  language bundles, and the `DEEPEVAL_METRIC_TEMPLATE_LANGUAGE` setting). V1
  ships base English templates only; revisit localization later. See progress.md.
